"use client";

/**
 * Compatibility adapter.
 *
 * FlowEngine is the only Companion runtime. This export remains temporarily so
 * historical imports cannot accidentally mount a second implementation.
 */
export { default } from "@/components/flow-engine/FlowEngine";
